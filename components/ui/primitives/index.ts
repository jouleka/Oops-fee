/**
 * UI Primitives - Reusable NativeWind components
 *
 * These components replace StyleSheet patterns with Tailwind classes.
 * Use these as building blocks for all mobile UI.
 */

// Card components
export { Card, CardContent, CardAccent } from './Card';

// Button components
export { Button, IconButton } from './Button';

// Badge components
export { Badge, CountBadge, StatusDot } from './Badge';

// Input components
export { Input, TextArea, InputGroup } from './Input';

// Chip components
export { Chip, ChipGroup, IconChip } from './Chip';

// Modal components
export { Modal, ModalHeader, ModalContent, ModalFooter } from './Modal';

